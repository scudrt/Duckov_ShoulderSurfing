<Project Sdk="Microsoft.NET.Sdk">

  <PropertyGroup>
    <TargetFramework>netstandard2.1</TargetFramework>
    <ImplicitUsings>enable</ImplicitUsings>
    <Nullable>enable</Nullable>
    <DuckovPath>F:\steam\steamapps\common\Escape from Duckov</DuckovPath>
    <LangVersion>10.0</LangVersion>
  </PropertyGroup>
  <ItemGroup>
        <Reference Include="$(DuckovPath)\Duckov_Data\Managed\TeamSoda.*" />
        <Reference Include="$(DuckovPath)\Duckov_Data\Managed\Cinemachine.dll" />
        <Reference Include="$(DuckovPath)\Duckov_Data\Managed\ItemStatsSystem.dll" />
        <Reference Include="0Harmony">
          <HintPath>libs\0Harmony.dll</HintPath>
        </Reference>
        <Reference Include="$(DuckovPath)\Duckov_Data\Managed\Unity*" />
  </ItemGroup>
  <ItemGroup>
    <Folder Include="ShoulderSurfing\" />
  </ItemGroup>
  <Target Name="PostBuild" AfterTargets="PostBuildEvent">
    <Copy SourceFiles="bin\Debug\netstandard2.1\ShoulderSurfing.dll" 
          DestinationFiles="F:\steam\steamapps\common\Escape from Duckov\Duckov_Data\Mods\ShoulderSurfing\ShoulderSurfing.dll" />
    <ItemGroup>
      <ResourceFiles Include="Resources\**\*" />
    </ItemGroup>
    <Copy SourceFiles="@(ResourceFiles)" 
          DestinationFolder="F:\steam\steamapps\common\Escape from Duckov\Duckov_Data\Mods\ShoulderSurfing\%(RecursiveDir)" 
          SkipUnchangedFiles="true" />
  </Target>
</Project>
